# v0.7.0 — Create spine: phase plan

> **Purpose.** The roadmap for *completing* the v0.7.0 milestone. This is a **multi-session** effort —
> it will not all land in one sitting — so this doc is the persistent handoff: what's done, what's next,
> who's needed, and in what order. Pairs with the **ratified design spec** (`docs/CREATE-SPINE.md`) and
> the integration PR (#234). Milestone board: [v0.7.0](https://github.com/theasshats/project-commonwealth/milestone/11).

## Where we are

- **Phase 1 (design) — DONE.** The Create-spine spec is **ratified** (cost model, power ladder + pacing,
  T3–T4 mod spine, aeronautics control ladder, boss-gate roster, MineColonies deferral). See
  `docs/CREATE-SPINE.md`. Implementation is the remaining work.
- **Phases 2–5 — pending.** Detailed below.

The v0.7.0 board is **15 issues**: #17, #84, #87, #92, #132, #145, #168, #171, #172, #187, #219, #220,
#231, #245, #258. (#185 folded into #187.)

## Legend

🔴 needs Xela + zagwar together · 🟡 maintainer review (splittable) · 🟢 solo + LLM-executable

---

## Phase 1 — Spec lock-in 🔴 — ✅ DONE

The four design decisions everything else builds against. **Ratified** in a working session and recorded
in `docs/CREATE-SPINE.md`.

- #219 cost model · #220 + #92 gating mechanism + lock list · #145 power tier ladder.

*Their **implementation** (apply the cost dials, build the KubeJS patterns, author the fork) lives in
Phase 3 — only the design is done.*

---

## Phase 2 — Merit calls 🔴 — PENDING

Fast keep/cut/which-wins verdicts that unblock execution. ~30–60 min of decisions; LLM executes each
result in minutes after.

- **#168** Alex's Mobs vs Naturalist — pick the winner per shared animal (bear choice breaks 3 Alex's recipes → rewire).
- **#87** SNA jetpack — keep-hidden vs cut (Create Jetpack stays canonical).
- **#84** scope — which Create-progression levers are v0.7.0 vs deferred to Aeronautics (v0.9.0).
- **#231** Create-armory gun balance — direction for ammo capacity / damage.

**Owner:** maintainers. **Est:** 1 short session.

---

## Phase 3 — Spine implementation 🟢 — PENDING (the bulk)

Build against the ratified spec. Gated on Phase 1 (done) and the Phase 2 verdicts. Highly LLM-accelerated.

**Foundation (do first — everything else costs against these):**
- **#219** — apply the cost dials (3× basics @ 2.5–3.5× band; step-depth ladder T1–T4) across all Create mods.
- **#220** — build KubeJS Pattern A (route-exclusive lock) + Pattern B (boss fork); lock the 2 exclusives (`precision_mechanism`, `electron_tube`).
- **#145** — author the power ladder gates + the per-generator converter-vs-free-source audit.

**Content (apply the patterns):**
- **#92** — the boss fork on the aeronautics controller (pick exact controller ID + boss drop in-PR).
- **#258** — Create: Nuclear end-game chain (boss-gated, step-depth staged).
- **#187** (+ folded #185) — gun path through sequenced assembly + the TaCZ non-gun bypass review.
- **#171** — Mob Lassos recipes through Create. · **#172** — Immersive Paintings graffiti through Create.
- **#84** — the Create-progression-harder levers (per Phase 2 scope). · **#87** — SNA balance.
- **#17** — running recipe-tweak list.
- **Aeronautics control ladder** — T1 balloon → T2 propelled/analog → T3 complex analog → T4 digital
  (`create-aeroworks`, added). T3–T4 mod spine (TFMG→New Age, Additions converter, Nuclear capstone).

**Owner:** solo + LLM, maintainer review per PR. **Est:** ~1–2 days of LLM-driven work across several PRs,
folded into #234 each with a playtest block.

---

## Phase 4 — Mod review sweep 🟡 — PENDING (parallel, anytime)

- **#245** — 27 Create/tech mods, 4-point rubric (loads / anchors / no overlap / sane config). Split the
  list between maintainers; LLM pre-fills per-mod load/anchor/overlap notes so the human pass is ticking +
  flagging. Cut/keep/weave outcomes spawn linked follow-ups.

**Owner:** maintainers (LLM-assisted). **Est:** 2–3 hrs human review, split. Not blocking — runs alongside.

---

## Phase 5 — Weave umbrella 🟢 — PENDING (LAST)

- **#132** — route the 54 Create addons together (almost-unified tags, `35-web-bridges.js`,
  `createaddoncompatibility`), then re-measure `--remove create` cohesion (currently 61%).

**Held until Phases 1–3 land** — it's the integration pass that encodes the cost model + gating into the
recipe graph; doing it earlier means rework. **Owner:** solo + LLM. **Est:** ~2–3 days iterative
(multi-pass, re-measuring connectivity each round).

---

## Critical path & sequencing

```
Phase 1 (done) ──► Phase 3 foundation (#219/#220/#145) ──► Phase 3 content ──► Phase 5 weave
                         ▲
Phase 2 verdicts ────────┘   (unblock #168/#84/#87/#231 execution)
Phase 4 sweep ── runs in parallel, non-blocking
```

- **Single biggest unblock:** Phase 2 verdicts + starting Phase 3 foundation (#219 cost dials). Once the
  cost model is applied, the rest of the recipe tail can be authored solo.
- **Exit criterion for v0.7.0:** all 15 issues implemented (close-on-stage as each lands on the branch),
  #234 playtest ticked on a fresh world, then merge → `release.yml` tags `v0.7.0`.

## Already landed this cycle (context)

Folded into #234 ahead of the spine work: the Create thunderdome curation (Let's Do cut, paintings
keep-one, Brazil Legends cut, Northstar pull), the dedup weave (#101/#102/#103), Game Discs routing
(#170), Dynamic Trees compat adds (Ars + Quark), Create: Aeroworks add, and the ratified spec itself.
Backlog trackers spun off: #270–#274.
