# Phase 2 candidates — chunk-09 (quality-audit: mods already ≥2 pillars)

## create   [anchors: Create (1 — IS the spine)]
OK — connections sound. Create is the method library, not a recipient. The dossier correctly marks it N/A as a weave target. No edges to audit.

## extradelight   [anchors: survival, Create (2)]
- REWORK: The dossier lists "already at 2 pillars" with `create:mixing` as the inbound weave. The `made-by` digest confirms `create:mixing` is live. However, the proposed deepen via `create:milling/crushing/pressing` for grain→flour / coffee grinding is listed as a candidate but not confirmed authored. The existing Create anchor is real (mixing is inbound), so the pillar count is correct. The M-12 deepening suggestion (milling grain, pressing citrus) is a valid REWORK target: the connection is single-method shallow and the pack's design goal says one is the floor, two is the goal — the Oven/Mortar chain should more visibly read as Create's downstream. Flag: ensure the Create anchor is backed by more than just one `create:mixing` call or the weave is thin.
- REWORK: The M-09 economy (feasts → Numismatics sell price) is noted as a candidate but not yet authored. The survival+Create pair is valid; economy is a bonus not a requirement. No action needed unless the 0.9 economy update targets this.

## occultengineering   [anchors: Create, magic (2)]
OK — connections sound. The `made-by` digest confirms `create:compacting`, `create:filling`, `create:haunting`, `create:item_application`, `create:mixing`, `create:pressing` (inbound from Create) AND `occultism:ritual`, `occultism:spirit_fire` (inbound from Occultism). Both pillars are strongly supported by multiple method inbounds. The weak M-08 economy note in the dossier is appropriately marked weak; sterling_silver coin is optional depth, not a gap.

## numismatics   [anchors: economy, Create (2)]
OK — connections sound. Numismatics is the hub destination for M-08/M-09; its Create pillar is structural (Create dependency, Create-styled crafting). Correctly identified as a destination node, not a source needing outgoing edges. No rework.

## aeronautics_bundled   [anchors: aeronautics (1 — IS the spine)]
- REWORK: Dossier claims aeronautics(1) with Create as "de-facto 2nd via component crafting." But the `made-by` digest shows NO inbound foreign methods — `(none / vanilla only)`. There is ZERO authored Create inbound. The "de-facto" label is aspirational, not real. The bundle wrapper registers 0 items/blocks, so strictly there is nothing to weave inbound; real content lives under the `aeronautics` namespace. This dossier's anchor claim of de-facto Create 2nd should be flagged — it needs an `aeronautics` dossier audit, not this wrapper. As a bundle with no content, it cannot hold any weave; the anchor count of 1 is honest, the parenthetical claim is misleading. Recommend the aeronautics ns dossier be reviewed for actual M-06/M-23/M-24 inbounds.

## aeronautics_dyeable_components   [anchors: aeronautics (1)]
- REWORK: Dossier correctly calls this a cosmetic skin layer with no strong 2nd pillar and recommends LEAVE. The `made-by` digest shows no inbound methods — vanilla only. The connection state is honestly documented. No weave authors here is correct. The dossier noting "could route dye via Create mixing" but calling it "gilding" is the right call — the M-12 anti-arbitrage guard applies (no basic dye step behind a machine). Confirm the pack's treatment: this mod sits at aeronautics(1) and is accepted as a pure cosmetic with no 2nd pillar required (cosmetic decoration mods are a sanctioned support role under the design north star). OK as documented.

## alcohol_industry   [anchors: Create, survival (2)]
- REWORK: `made-by` confirms `create:filling` and `create:mixing` are live inbound. Both Create methods are real. Survival (drinkable consumables with effects) is the second anchor. The M-09 economy deepening (bottled spirits as sellable luxury) is proposed but not authored — this is a genuine gap opportunity, not a defect in the current connections. The existing connections are sound: Create spine is properly woven via the fluid/mixing chain. The economy deepening is a good candidate for the 0.9 economy update; flag for that milestone. No rework of existing connections needed.

## ars_nouveau   [anchors: magic (1)]
- REWORK: Dossier lists magic(1) only — but the pack's design requires a 2nd pillar. The `made-by` shows only `(none / vanilla only)`. No inbound weaves from Create/economy/survival are authored yet. This is a **missing weave gap**, not a quality issue with existing connections. The M-01 source_gem currency hub and M-02 mob-drop→magic sink are proposed candidates but neither is authored yet. At 1 pillar, this is below the floor. Flag: Ars Nouveau needs at least one of M-01/M-02/M-10 authored before the Create-spine milestone. This is the most significant gap in this chunk.

## astikorcartsredux   [anchors: survival (1)]
- REWORK: Dossier correctly shows survival(1) only and notes no strong 2nd pillar. `made-by` shows vanilla only. The dossier's own assessment is "none strong — leave." With the design rule that one pillar is the floor and two is the goal, this mod is at the floor with no authored 2nd. The M-05 gating suggestion (Create-processed iron sheets for plow blades) is weak but not zero — it would give a thin Create anchor. This is not a defect in existing connections (there are none to audit) but it is a gap in the pillar coverage. Leave for now per dossier recommendation; revisit if the survival pillar milestone wants more coverage.

## bits_n_bobs   [anchors: Create, survival (2)]
OK — connections sound. `made-by` confirms `create:item_application` and `create:splashing` as live inbound. The Create anchor is real. Survival/decoration is the second anchor. The M-04 deco→crushing deepening is noted as optional. No rework of existing connections needed.

## cbc_at   [anchors: Create, aeronautics (2)]
OK — connections sound. `made-by` confirms `create:cutting` and a full suite of `createbigcannons:*_assembly` inbounds. Both Create and aeronautics pillars are strongly supported. The M-05 boss-key gating suggestion for heavy-autocannon tier is a design-depth note, not a missing pillar. No rework.

## chefsdelight   [anchors: survival, economy (2)]
- REWORK: `made-by` shows `(none / vanilla only)` — no authored inbound methods. The economy anchor is claimed via villager trades (emerald-based), but these are vanilla trade mechanics, not a Numismatics-woven economy connection. The "economy" pillar here is emerald-economy, which is vanilla, not Derpack X's Numismatics economy pillar. This is a **pillar-count inflation**: survival(1) is real; economy(2) via villager emerald trades is arguably not the same "economy" pillar as the pack defines (Numismatics + trade coin). The dossier itself flags "route its trades onto numismatics coins instead of emeralds" as needed for a real connection — that's the tell. Recommend: treat as survival(1) until M-21 trade-seam or M-09 coin pricing is authored, OR clarify in the dossier that "economy" here means loose thematic adjacency, not a Numismatics weave. Either way the description should not count the emerald trade as the pack's economy pillar without a Numismatics hook.

## cmpackagecouriers   [anchors: Create, aeronautics (2)]
OK — connections sound. `made-by` confirms `create:cutting` and `create:deploying` are live inbound. The Create anchor is real. The aeronautics/logistics anchor (autonomous flying cardboard courier) is inherent. No rework.

## companions   [anchors: survival (1)]
- REWORK: Dossier lists survival(1) only — same as ars_nouveau, this is a 1-pillar mod below the floor. `made-by` shows no inbound methods. The M-11/M-02 magic bridge (Crystallized Blood / soul gems → Occultism or Ars) and M-09 economy bridge are both listed as candidates but neither is authored. This is a **missing weave gap**. The soul_furnace method is a strong connective surface (mob-drop→reagent) that maps naturally to M-11 or M-02. The mod's internal coin system competing with Numismatics is a risk worth noting: if companions:copper_coin / nether_coin / end_coin exist as separate currencies and interact with the same trade space, there is a design conflict with Numismatics as the pack's sole economy hub. Recommend explicitly noting in the dossier whether the internal coins should be blocked from the Numismatics trade layer or bridged to it. The magic weave (M-11) is the higher-priority action.

== CHUNK COMPLETE ==
