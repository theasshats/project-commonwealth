# Phase 2 candidates — chunk-09
<!-- quality-audit pass: mods already ≥2 pillars; list REWORK items or OK -->

## create   [anchors: Create (1 — IS the spine)]
- OK — Create is the method library and intentional hub, not a recipient of weaves. Dossier correctly marks it N/A as a recipient. No connections to audit; the design is sound.

## extradelight   [anchors: survival, Create (2)]
- REWORK: The existing inbound weave from `create:mixing` is correct and sound (M-12 processing-chain pull). However, the connection description is thin — the dossier says "partially Create-woven" but leaves the depth under-specified. The Vat/Evaporator fluid-reduction steps (e.g. salt, syrups) are natural targets for `create:mixing` → `extradelight:evaporator`/`extradelight:vat` chaining, but this isn't formally stated as a route. Suggest specifying: grain → `create:milling` → flour → `extradelight:flour_dough`/`extradelight:oven` as the canonical Create→extradelight processing chain (M-12), so the bakery reads downstream of the Create grain-mill, not as an independent kitchen. One concrete recipe route would crystallize the existing vague "partially woven" into a real edge.
- REWORK: Economy pillar is absent. The dossier flags M-09 (luxury good→coin) as a "MED" candidate for feasts/dishes as sellable goods. Given that extradelight produces the pack's widest cooked-food catalog — including feasts — and M-09 is accepted, the absence of a numismatics sell-price/vendor assignment for at least one feast tier is a gap. This is a low-cost KubeJS/config edit (set a vendor price on high-effort feasts), not a deep recipe chain. Recommend adding this as a concrete economy edge so the mod reaches a genuine two-web presence rather than a single-web Create+survival cluster.

## occultengineering   [anchors: Create, magic (2)]
- OK — The existing connections are sound and tight. Made-by methods cover `create:compacting`, `create:filling`, `create:haunting`, `create:item_application`, `create:mixing`, `create:pressing`, `occultism:ritual`, `occultism:spirit_fire` — a genuine two-web presence with Create kinetics on one side and Occultism rituals on the other. The `spirit_solution` reserved-reagent rule is correctly noted in the dossier. The weak M-08 economy candidate (sterling_silver→coin) is correctly flagged as WEAK and not forced. No rework needed.

## numismatics   [anchors: economy, Create (2)]
- OK — Numismatics is the economy-pillar keystone and correctly treated as a destination, not a source. The dossier explicitly says "don't add an outgoing edge" and the design rationale is sound. Its Create dependency gives it a natural Create anchor. No connections to audit; the design is intentional and correct.

## aeronautics_bundled   [anchors: aeronautics (1 — IS the aeronautics spine)]
- REWORK: The dossier lists anchors as aeronautics (1) with a note that Create is "de-facto 2nd via component crafting." But the bundle jar registers 0 blocks, 0 items, 0 recipe methods — it is a wrapper; actual content lives under the `aeronautics`/`sable` namespaces. The dossier itself says "weaving belongs on the `aeronautics` namespace dossier, not this bundle wrapper." The quality-audit concern here is that aeronautics_bundled is in the ≥2 pillar chunk despite being a pure wrapper with no auditable connections. It should be re-classified as infrastructure (no independent audit needed) and the real `aeronautics` dossier should carry the pillar assessment. As-is, counting it at 2 pillars is inaccurate — the wrapper adds no content. Flag for dossier correction: move pillar count to `aeronautics` ns dossier; mark aeronautics_bundled as "distribution wrapper — no independent connections."

## aeronautics_dyeable_components   [anchors: aeronautics (1)]
- OK — The dossier correctly identifies this as a cosmetic skin layer (dyed levitite/tires) with no strong 2nd-pillar candidate. The analysis — "forcing a Create/economy edge would be a non-edge" — is sound. One could theoretically route the dye step through `create:toolbox_dyeing` or `create:mixing`, but that would add zero gameplay value; the vanilla dye+craft path is already the right form for a cosmetic. LEAVE is the right call. No rework needed; the existing single-pillar classification is honest and correct.

## alcohol_industry   [anchors: Create, survival (2)]
- REWORK: The existing Create inbound weaves (`create:filling`, `create:mixing`) are confirmed and correct — the Alcohol Base is assembled through Create, establishing the Create anchor. The survival anchor is real (drinkable consumables with status effects). However, the economy connection is noted as STRONG in the dossier (M-09 luxury good→coin for bottled spirits) but is not yet wired. For a pack built around emergent specialization, a dedicated distillery producing vendable spirits is a natural economy node — and the absence means the mod's output (beer/vodka/whiskey/tequila) has no downstream economic role. Recommend adding at least one Numismatics vendor price assignment for bottled spirits so the distillery chain has an economy endpoint. This is a config/KubeJS line, not a deep recipe. No structural rework needed — just close the flagged gap.

## ars_nouveau   [anchors: magic (1)]
- REWORK: The dossier lists magic (1) as the only anchor, and lists 2nd-pillar candidates as M-02 (mob-drop via Vitalic/Agronomic Sourcelink) and M-01/M-10 (imbuement hub). However, the made-by methods show "(none / vanilla only)" — meaning no foreign mod currently routes material INTO ars_nouveau via a registered method. The Sourcelink passive-sink (crops/mob deaths feeding Source) is a non-recipe mechanic and is real, but it isn't surfaced as a formal weave. This is a classification issue: if ars_nouveau is in the ≥2 pillar chunk, what is its 2nd pillar? The dossier says magic (1), suggesting it was included by mistake or is borderline. The Vitalic/Agronomic Sourcelinks do connect it to the survival pillar organically (farm/mob output → Source generation), which justifies survival as a de-facto 2nd. But this is nowhere formalized as a weave. REWORK: (a) Confirm whether ars_nouveau belongs in this quality-audit chunk at all — if it is genuinely at 1 pillar, it belongs in a 1-pillar chunk for new-weave proposals. (b) If counting Sourcelinks as survival anchor, document it explicitly and add one concrete weave (e.g. Serene Seasons seasonal crop → Agronomic Sourcelink = M-16 seasonal reagent, magic↔survival). This would be a meaningful and cheap 2nd-pillar confirmation.

## astikorcartsredux   [anchors: survival (1)]
- REWORK: The dossier marks astikorcartsredux as survival (1) — a single-pillar mod. It should not be in this quality-audit chunk (which is for mods already ≥2 pillars). Either the chunk list is wrong, or the dossier understates its pillars. Reviewing the content: horse-drawn carts and farm implements (plow/reaper/seed-drill) for bulk crop harvesting and hauling. The only anchor is rustic-agrarian survival. No registered methods, no inbound weaves, no Create/economy connection. This is correctly a 1-pillar mod. Flag: astikorcartsredux appears mis-assigned to this chunk; it needs a 2nd-pillar proposal, not an audit. The dossier's own "LEAVE" recommendation (no strong 2nd candidate) is defensible but deserves a second look: the reaper/plow-blade could plausibly gate on Create-pressed iron sheets (M-05 native-method gating, everyday tier → one light step), giving it a Create anchor without over-engineering.

## bits_n_bobs   [anchors: Create, survival (2)]
- OK — The existing connections are sound. Made-by methods (`create:item_application`, `create:splashing`) confirm Create as a real inbound anchor, and the broad deco palette justifies the survival/build anchor. The M-04 candidate (deco tiles crushable back to crushed-stone via `create:crushing`) is a sensible reinforcement but not missing — it's noted as an optional deepening. The chain-pulley's kinetic role inside Create builds is genuine. No rework needed.

## cbc_at   [anchors: Create, aeronautics (2)]
- OK — The existing connections are tight and genuine. Made-by methods confirm Create (`create:cutting`) and createbigcannons (multiple munition-assembly types) as real inbound weaves. The mod's content — autocannons, rocket pods, barrel attachments — is purpose-built for arming Aeronautics contraptions. The Create↔aeronautics two-pillar reading is accurate. The M-05 deepening suggestion (boss-drop gate for heavy tier) is a good optional progression note, not a structural gap. No rework needed.

## chefsdelight   [anchors: survival, economy (2)]
- REWORK: The economy anchor rests on villager trades using vanilla emeralds, not Numismatics coins. In a pack where Numismatics is the designated economy pillar, emerald-denominated trades are a parallel economy track that doesn't connect to the coin web. The dossier correctly flags this: "route its trades onto numismatics coins instead of emeralds (M-09)" as a MED candidate. This is the one meaningful rework: the Cook/Chef villager trade output should be expressible as Numismatics coin income (via KubeJS economy bridge or a Numismatics vendor seeded with the same goods), so the food↔economy connection lands inside the pack's actual economy layer rather than the vanilla-emerald silo. Without this, the "economy" anchor is technically present but doesn't weave into the pack's economy web — it's emerald-island.

## cmpackagecouriers   [anchors: Create, aeronautics (2)]
- OK — The existing connections are sound. Made-by methods confirm `create:cutting` and `create:deploying` as real inbound weaves (the cardboard plane is assembled through Create). The aeronautics anchor is legitimate: autonomous flying couriers are a logistics arm of the aeronautics pillar. The economy adjacency note (remote trade fulfilment) is correctly flagged as "leave unless trade tooling wants it" — not a gap, just an option. No rework needed.

## companions   [anchors: survival (1)]
- REWORK: Like astikorcartsredux above, companions is listed in the ≥2 pillar quality-audit chunk but the dossier shows survival (1) as its only anchor. Made-by methods: "(none / vanilla only)" — no inbound weaves exist. This is a 1-pillar mod that appears mis-assigned to this chunk. The dossier identifies two strong 2nd-pillar candidates: (1) magic via occultism:spirit_fire / ars_nouveau:imbuement for Crystallized Blood / soul gems (M-11 ritual/transmutation sink, M-02 mob-drop reagent sink) — genuinely strong, thematically coherent (gothic-alchemical vibe maps directly onto Occultism's eldritch tone); (2) economy via numismatics for Relic Gold / Sacred Pontiff drops (M-09 luxury good→coin). The first is the priority: routing Soul Furnace outputs (soul gems, essences) into Occultism rituals or Ars imbuement would give companions a real magic anchor and connect its internal loop to the pack's magic web. The competing coins (copper/nether/end) should be addressed — either deprecate in favor of Numismatics or bridge them, else two coin systems compete.

== CHUNK COMPLETE ==

