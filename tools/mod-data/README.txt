Mod-data digest — IDs / tags / biome-modifiers only (no recipes/loot/lang/assets).
Generated by scripts/extract-mod-data.sh over 354 jars.

  by-mod/<jar>.txt            per-mod blocks, items, biome_modifiers, c: tags, deps
  recipes/<jar>.txt           per-recipe 'id | type | referenced ids' (derived index, not verbatim)
  loot/<jar>.txt              per-loot-table 'id | type | referenced ids' (derived index, not verbatim)
  INDEX-ores.txt              every *_ore blockstate across all mods (data-driven census)
  INDEX-biome-modifiers.txt   every neoforge biome_modifier across all mods

Note: catches DATA-DRIVEN worldgen only; pair with the in-game EMI ore plugin for
ores a mod injects in code (no biome_modifier json).
