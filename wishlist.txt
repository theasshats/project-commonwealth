# Mod wishlist
#
# Add lines below for mods you want installed. When you commit, the
# "Sync wishlist" workflow installs them and clears the entries.
# Next time you open this file, it'll be empty again.
#
# Format:
#   source:slug          (defaults to side=both)
#   source:slug:side     (side = both | client | server)
#
# Where source is:
#   mr  -- Modrinth      (e.g. mr:jei)
#   cf  -- CurseForge    (e.g. cf:create-aeronautics-compatability)
#
# Examples:
#   mr:jei
#   mr:xaeros-minimap:client
#   cf:create-aeronautics-compatability
#
# To remove a mod from the pack, use the "Remove mod" workflow instead.
#
# === Add new mods below this line ===
mr:handcrafted